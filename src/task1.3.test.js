/* eslint-disable no-console */

import { getSumOfNumbers } from "./task1.3";

describe("sum", () => {
  it("returns sum of numbers from 1 till num", () => {
    expect(getSumOfNumbers(1, 2)).toBe(console.log(3));
  });
});
